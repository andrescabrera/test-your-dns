# Graph the DNS test results

## install the dependencies

`sudo apt install python`

`pip install requirements.txt`

## Then run the grapher script
`python graph-dns-results.py`

## Check for the results:
ip_milliseconds_chart.json

# Take the results to Vega JSON grapher
https://vega.github.io/editor/#/edited
